services:
  - type: web
    name: sistema-acceso-frontend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: starter
    envVars:
      # VITE_API_URL es OPCIONAL - el código ya tiene la URL de producción por defecto
      # Solo configúrala si necesitas usar una URL diferente
      # - key: VITE_API_URL
      #   sync: false
      #   value: https://project-pas-api.onrender.com

