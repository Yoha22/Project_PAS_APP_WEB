services:
  - type: web
    name: sistema-acceso-frontend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: starter
    envVars:
      - key: VITE_API_URL
        sync: false
        # IMPORTANTE: Configura esta variable en Render > Environment
        # Ejemplo: https://project-pas-api.onrender.com
        # (sin /api al final, se agrega automáticamente)

